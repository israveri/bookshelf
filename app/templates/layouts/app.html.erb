<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bookshelf</title>
    <%= favicon_tag %>
    <%= stylesheet_tag "app" %>
  </head>
  <body>
    <% [:alert, :notice].each do |level| %>
      <% if flash[level] %>
        <p><%= flash[level] %></p>
      <% end %>
    <% end %>

    <%= yield %>
    <%= javascript_tag "app" %>
  </body>
</html>
